# This extension has no build step and no dependencies. Nothing generated by a
# tool belongs in the repo, except icons/, which are committed so "Load unpacked"
# works straight from a clone.
node_modules/
*.log
*.tmp
*.zip
*.crx
*.pem
.DS_Store

# Anything a verification run might leave behind.
downloads/
*.pdf
